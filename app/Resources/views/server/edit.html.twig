{% extends 'base.html.twig' %}

{% block body %}
    <h1>Server edit</h1>

    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    Edit Serveur
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-6">


                            {{ form_start(edit_form) }}


                            <div class="form-group">
                                <label>Nom</label>
                                {{ form_widget(edit_form.nom, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group">
                                <label>Emplacement</label>
                                {{ form_widget(edit_form.emplacement, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                            <div class="form-group">
                                <label>Modele</label>
                                {{ form_widget(edit_form.modele, { 'attr': {'class': 'form-control'} }) }}
                            </div>

                            <button type="submit" class="btn btn-default">Edit</button>
                            {{ form_end(edit_form) }}


                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}














