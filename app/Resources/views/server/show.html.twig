{% extends 'base.html.twig' %}

{% block titre %}Details Serveurs{% endblock %}

{% block body %}

<!-- /.row -->
<div class="row">

    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                détails serveurs
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">

                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ server.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ server.nom }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ server.emplacement }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ server.modele }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('server_index') }}"><button class="btn btn-green btn-md" >Back to the list</button></a>
        </li>
        <li>
            <a href="{{ path('server_edit', { 'id': server.id }) }}"><button class="btn btn-green btn-md" >Edit</button></a>
        </li>
        <li>
            <button class="btn btn-green btn-md" >
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
            </button>
        </li>
    </ul>
{% endblock %}
